`simpler_tasks` remains the one mechanism that needs a lower learning rate in this replication setup: after fixing label alignment and reducing the shared profile to `1e-5`, we ablated longer warmup at the same learning rate and found that `warmup=8` stabilized its synthetic pre-pretraining but did not prevent transferred downstream divergence, which reappeared around step `40/80` with evaluation loss already in the hundreds, so `simpler_tasks` keeps study-specific `1e-6` synthetic and downstream overrides while the shared profile uses the higher common rate.
